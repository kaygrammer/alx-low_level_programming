#include "main.h"
#include <stdlib.h>

/**
 * _abs - check for absolute value.
 *
 * @n : the character to check
 *
 * Return: 1 if it's lower && 0 if it's not.
 */

int _abs(int n)
{
return (abs(n));
}
